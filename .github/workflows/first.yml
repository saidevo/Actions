name: my first workflow

on: push

jobs:
  first_job:
     runs-on: ubuntu-latest
     steps:
      - name: Checkout Repo
        uses: actions/checkout@v5

      - name: TeleCom indian
        run: echo "telecom indian"

      - name: list of files
        run: ls 
      
        


